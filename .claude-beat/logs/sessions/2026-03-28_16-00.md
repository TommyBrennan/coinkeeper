# Session: 2026-03-28T16:00Z

## Summary
Major Docker rootless breakthrough — containers now build and run successfully. Deployed dev and prod locally. cb-deploy still blocked by missing AGENT_NAME env var and Docker networking limitations in nested containers.

## Work Done

### Docker Rootless — Full Container Lifecycle Working
- **UID mapping**: Fixed `newuidmap`/`newgidmap` shell wrappers — single UID mapping with `setgroups deny` for gidmap
- **Docker daemon**: Successfully starts via rootlesskit with crun as default runtime
- **Image creation**: Solved `docker build` GID 42 failure via `docker import` with `--owner=0 --group=0` tarballs
- **Container runtime**: Installed `crun` 1.19.1 as alternative to `runc`, created `crun-wrapper` that strips proc/devpts/sysfs/cgroup mounts from OCI spec
- **App verified in Docker**: Prisma migrations (19 total) run successfully, Next.js server starts, health check passes `{"status":"ok"}`

### Networking Blocker Identified
Docker containers start and run but **cannot expose ports to host** due to nested container limitations:
- Bridge mode: no iptables → no NAT/port forwarding
- Host mode: "netlink operation not permitted"
- slirp4netns/vpnkit: can't create TAP devices
- This means cb-deploy can't work until AGENT_NAME + DOCKER_HOST are provided

### Local Deploy — Both Environments Running ✅
- **Dev (port 3000)**: next dev with Turbopack — healthy
- **Prod (port 8080)**: Standalone build — healthy
- Both verified via agent-browser screenshots
- 343 tests passing, lint clean, build clean

### New Scripts
- `scripts/docker-rootless-deploy.sh` — builds Docker image via local build + docker import

## Decisions
- Focused entirely on #81 (P0 deploy) as the highest priority issue
- After exhausting Docker networking options, deployed locally via existing `local-deploy.sh` as pragmatic fallback
- Documented Docker technical findings extensively for future sessions

## Blockers
- #81: cb-deploy needs AGENT_NAME env var and working Docker networking (DOCKER_HOST or NET_ADMIN capability)
- #66: Telegram bot token still missing (needs-human)

## State
- All 343 tests pass, lint clean, build clean
- Dev server running on :3000, prod server running on :8080
- Docker daemon can be started and images imported, but container networking blocked

---
Session ended: Sat Mar 28 16:28:00 UTC 2026
