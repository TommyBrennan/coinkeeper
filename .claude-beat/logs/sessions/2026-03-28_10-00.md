# Session Log — 2026-03-28T10:00Z

## Summary

Focused on issue #81 (P0 deploy). Downloaded Docker static binaries and got rootless Docker daemon running, but image builds still fail due to kernel UID namespace limitations. Deployed app locally: dev on port 3000, prod on port 8080, both healthy and styled. Browser-verified with agent-browser.

## Work Done

### Docker rootless daemon setup
- Downloaded Docker static binaries (dockerd, containerd, rootlesskit, slirp4netns) for aarch64
- Created proper newuidmap/newgidmap shell scripts for multi-UID mapping
- Successfully started rootless Docker daemon via `rootlesskit --net=host --copy-up=/run`
- **Image builds still fail**: `lchown` for GID 42 (shadow group) returns "invalid argument" due to single-UID namespace
- Root cause: no CAP_SETUID capability and no setuid-root newuidmap binary — cannot map multiple UIDs/GIDs

### Local deployment — BOTH RUNNING
- **Dev (port 3000)**: `next dev` — auto-reloads on changes
- **Prod (port 8080)**: Standalone build — restarted with latest code + static assets
- Fixed missing CSS in prod: copied `.next/static` to `.next/standalone/.next/static`
- Both servers pass health checks, auth redirects work correctly

### Browser verification (agent-browser)
- Login page: properly styled with CoinKeeper logo, email field, passkey button
- Register page: properly styled with name, email fields
- Dev and prod look identical
- Screenshots saved in `.claude-beat/logs/screenshots/`

### Deploy script
- Created `scripts/local-deploy.sh` for managing local dev/prod servers

## Deploy Status

- **Local dev (port 3000)**: HEALTHY
- **Local prod (port 8080)**: HEALTHY
- **cb-deploy (Docker)**: BLOCKED — no AGENT_NAME, no CAP_SETUID for multi-UID Docker builds

## Test Results

- 309 tests passing (18 test files)
- Lint clean, build clean

## Issues

- #81 (deploy) — commented with detailed Docker analysis and local deploy status

## Next Session

- Monitor deploy issue #81 for infrastructure changes (AGENT_NAME, Docker access)
- Look for approved issues or PRD features to implement
- Check if any new issues or PR comments need attention

---
Session ended: Sat Mar 28 10:12:00 UTC 2026
