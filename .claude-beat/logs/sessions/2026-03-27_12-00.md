# Session Log — 2026-03-27T12:00Z

## Summary
Deployed dev and prod instances locally via standalone Next.js (PR #119, merged). Extensively tested Docker/cb-deploy — confirmed fundamentally blocked by container kernel limitations. Fixed health check auth bypass in middleware.

## Review Findings
- #81 Deploy: Issue title updated to "URGENT" with "infrastructure is unblocked now" — investigated thoroughly
- Docker daemon can start via rootlesskit `--net=none` but has no network (no /dev/net/tun for TUN/TAP)
- Docker containers can't start (can't mount /proc in nested namespaces)
- App already had running dev servers from previous sessions
- All features complete, 136 tests pass

## Work Done
1. **Docker investigation** — exhaustive testing of all rootless Docker approaches:
   - slirp4netns: fails (no /dev/net/tun)
   - vpnkit: fails (no /dev/net/tun)
   - pasta: not installed
   - `--net=none`: daemon starts but no network → can't pull images
   - Created custom base image from host filesystem, imported successfully
   - Containers fail to start: can't mount /proc (namespace restriction)

2. **Local deploy script** (PR #119, merged)
   - `scripts/deploy-local.sh` with dev/prod/stop/status/logs commands
   - Dev on port 3000, prod on port 8080
   - Automatic Prisma migration handling, health checks, PID management
   - Added `.deploy/` to `.gitignore`

3. **Middleware fix** (PR #119, merged)
   - `/api/health` now accessible without authentication
   - Enables proper health monitoring of deployed instances

4. **Verification**
   - Both instances healthy: `{"status":"ok","checks":{"database":"ok","migrations":"ok"}}`
   - Auth API endpoints working (WebAuthn registration/login verified)
   - Login and register pages render correctly via agent-browser
   - All 136 tests pass, build clean, lint clean

## Decisions
- Deployed locally without Docker since cb-deploy is fundamentally blocked
- Used standalone Next.js output for production-like deployment
- Both dev and prod share the same SQLite database at `/app/data/coinkeeper.db`

## Blockers
- #81 cb-deploy: Container needs `--device /dev/net/tun --cap-add SYS_ADMIN` + AGENT_NAME + DOCKER_HOST
- #66 Telegram bot token: needs-human

## Next Session
1. Check if container capabilities changed (for Docker/cb-deploy)
2. Consider additional test coverage or accessibility improvements

---
Session ended: Fri Mar 27 12:25:00 UTC 2026
